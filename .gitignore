# Generated files
*_templ.go
static/css/style.css
style.css.map

# Binaries (bin/ is `make all`; root-level are stray `go build` outputs)
bin/
tmp/
/server
/muni
/munisign
/fetcher
/summarize
/auditbudget
/buildshapes
/perftest

# Secrets
secrets.conf
.env

# Tools
.claude
node_modules

# Generated FIR budget data (regenerate with `./bin/fetcher budget`)
static/budget/fir_*.json

# Council minutes PDFs, reports, and extracted votes (regenerate with `./bin/fetcher votes`)
static/councillors/minutes/
static/councillors/reports/
static/councillors/votes_*.json

.playwright-cli
/perftest

# Runtime data — extracted artifacts, downloaded bundles, never code.
# Anchored to project root so it doesn't shadow internal/data/ (Go package).
/data/

# Database backups (./scripts/backup.sh writes here)
backups/

# Caddy access logs (bind-mounted by docker-compose.prod.yml, rotated daily, max age 7 days)
caddy-logs/
